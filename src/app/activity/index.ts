import ActivityRepository from './repository';
import ActivityRoute from './route'

const ActivityFolder = {
    route: ActivityRoute,
    repository: ActivityRepository
};

export default ActivityFolder;